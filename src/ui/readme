dynamic/ is the web half of RootView: the dashboard and introspection GUI that
runs on the KVM host alongside the tool. See dynamic/README.md to run it.

The public course page -- the one we present, with the deliverables index --
is a separate static site in its own repository, published with GitHub Pages.
It is not built from here.
