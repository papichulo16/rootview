"""RootView web server.

The user-facing half of RootView: a dashboard and introspection GUI over the
KVM/LibVMI engine that inspects a guest's eBPF subsystem from outside the
guest.
"""

__version__ = "0.1.0"
